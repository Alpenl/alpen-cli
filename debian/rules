#!/usr/bin/make -f

# 跳过 buildinfo 生成,避免在 CI 环境中挂起
export DEB_BUILD_OPTIONS=nocheck

%:
	dh $@

override_dh_auto_build:
	go build -o alpen -ldflags="-s -w" .

override_dh_auto_install:
	install -D -m 0755 alpen debian/alpen-cli/usr/bin/alpen

# 跳过 buildinfo 文件生成
override_dh_builddeb:
	dh_builddeb
