# Alpen CLI 全局配置
#
# 字段说明：
# - defaultConfigPath: 默认命令配置文件的绝对路径。
# - scriptsRoot:       全局脚本仓库目录，命令可引用此处的脚本。
# - searchPaths:       额外的配置文件搜索目录，按顺序优先级匹配。
#
# 提示：
# 1. 可通过 `alpen env` 交互式切换配置，`alpen env --reset` 恢复默认值。
# 2. 若需要共享配置，请将 demo.yaml 推送至团队仓库，并在此处更新。
# 3. scriptsRoot 下可根据需要新增子目录（deploy/tests 等），保持结构清晰。
# 4. 同级目录可创建 config/demo.<env>.yaml 以支持环境覆写。
#
# 以下示例值会在 `alpen init` 时自动渲染，可按需修改。

defaultConfigPath: "{{ .DefaultConfigPath }}"  # 默认命令配置文件
scriptsRoot: "{{ .ScriptsRoot }}"                # 全局脚本仓库

# 额外搜索路径（按顺序匹配）
searchPaths:
{{- range .SearchPaths }}
  - "{{ . }}"
{{- end }}
